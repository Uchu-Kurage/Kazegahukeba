# Kazegahukeba（風が吹けば）

終わりゆく世界の最後の夏（8/31 に世界が終わる）を過ごす、2Dドットの ADV 寄り RPG。
このリポジトリは **まず「一日が回る骨組み」だけ** を動かすための最小構成です。
物語・キャラ・ドット絵・エンディングは、この骨組みが動いてから足していきます。

## いま動くもの（最小の核）

マップは2階層です。街は歩かず、地図上の場所を選ぶ「選択層」。中に入ると歩けます。

```
【タイトル Title】はじめる／エンディング記録／記録を消す
        ↓ はじめる
【街全体マップ Town】どこへ行くか選ぶ（歩かない・カーソル選択）
   矢印/WASD で場所アイコンを選び ［E］
        ↓ 中へ入る
【各場所マップ Place】2Dドットで歩き回る
   中の人に近づいて［E］＝会話（画面下のテキストボックス。専用の立ち絵は使わず、
   マップ上のドット絵キャラのまま進む）→ 会話が終わるとこの枠を消費／出口［E］で選び直し
        ↓ 過ごしたら街へ戻る
   午前 → 午後 → 夜（振り返り／街で［E］めくる）→ 翌日 …（40日で世界の終わり）
        ↓ 8/31 を越える
【エンディング Ending】結末を再生 → 到達を記録 → 全到達で裏エンド → タイトルへ
```

- 40日間のカレンダー制（8/1 起点、`GameState.TOTAL_DAYS` で日数を調整可能）
- 一日 = 午前 / 午後（行動枠）＋ 夜（基本は振り返り）
- 街で行き先を **選択** → その場所の中へ移動して探索 → 過ごすとその枠を消費（他へは行けない）
- 過ごさずに出口から戻れば、同じ枠で別の場所を選び直せる
- 予定なしのスキップ（街で［Q］。枠は消費する）
- 場所に紐づくキャラの関係値を +1 する仮ロジック（`GameState.affinity`）
- 40日を越えると「世界の終わり」→ もう一度夏を始める（周回）

## 操作

| キー | 動き |
|------|------|
| WASD / 矢印キー | 街：行き先カーソルの移動 ／ 場所の中：歩行 |
| E（Space / Enter） | 中へ入る・話す・会話送り・カレンダーをめくる・戻る・周回リスタート |
| Q | 街：予定なしでスキップ ／ 場所の中：過ごさず街へ戻る |

## 動かし方

1. **Godot 4.6** でこのフォルダ（`project.godot` のある場所）を開く。
2. 初回はアセットのインポートが走る（少し待つ）。
3. ▶ 実行（F5）でメインシーン `scenes/Town.tscn` が起動する。

> **見た目は仮です。** プレイヤーも場所も、いまは四角い「プレースホルダ」で描いています
> （アートが無くても動かせるように）。ドット絵は後から `Sprite2D` /
> `AnimatedSprite2D` / `TileMapLayer` に差し替えていきます。

> **日本語フォント**は `assets/fonts/NotoSansJP-Regular.ttf`（Noto Sans JP / OFL 1.1）を
> 同梱し、`project.godot` の `gui/theme/custom_font` で全体に適用しています。

## ファイル構成

```
project.godot              プロジェクト設定（Autoload登録・メインシーン・フォント）
autoload/
  GameState.gd             ★状態の中心。日付・時間帯・好感度・フラグ＋一日を進める状態機械
  Controls.gd              操作キーの登録（起動時に一度）
  SaveData.gd              セーブ（周回記録＝到達エンド・周回数／進行中の1周＝中断・再開）
  AudioManager.gd          BGM／効果音（音源が無ければコード合成で鳴らす）
  Nav.gd                   シーン遷移（街 ⇄ 場所 ⇄ エンディング）の入口
  Dialogue.gd              会話ウィンドウ（画面下テキストボックス。立ち絵なし・選択肢対応）
data/
  Locations.gd             町の「場所」定義（id/名前/キャラ=route_id/街マップ上の座標/アイコン）
  Routes.gd                三ルート（球磨=kuma／由布=yufu／葵=aoi）を同じデータ構造で定義（節目＝前提×時期×台本）
  Timeline.gd              共通背景タイムライン（中盤の球磨離脱フラグ＝全ルート共通／葵の日替わり居場所）
  Story.gd                 三ルート共通のイベント解決器（§4 の優先順位／フラグ条件ノードの展開）
  Dialogues.gd             会話データ（ルート×節目の台本・日常filler・葵の遍在遭遇。テキストは仮置き）
  Endings.gd               三ルート対応のエンディング判定（立場×関係値×到達節目数）と結末台本
scenes/
  ExploreMap.gd            歩き回れるマップの共通土台（Place の親クラス）
  TownBackground.gd        街の地図背景を _draw() で描画（草地・川・田んぼ・道）
  PlaceIcon.gd             街の地図の場所看板（丸看板＋アイコン＋名前、選択で脈動）
  Title.tscn / Title.gd    タイトル画面（メインシーン）。はじめる／記録／記録を消す
  Town.tscn / Town.gd      街全体マップ。地図で行き先を選ぶ層（歩かない）
  Place.tscn / Place.gd    各場所の中のマップ。歩いて中の人と過ごす層（1つで全場所を兼ねる）
  Ending.tscn / Ending.gd  エンディング画面。結末を流し、到達を記録し、周回へ
  Player.tscn / Player.gd  2D操作キャラ（CharacterBody2D）。場所の中で使う
  LocationSpot.tscn / .gd  対象（Area2D）。中のNPC（ドット絵キャラ）・出口を兼ねる
  CharacterArt.gd          ドット絵スプライトをコード生成（配色から SpriteFrames を作る）
  PixelCharacter.gd        AnimatedSprite2D。待機／歩き × 下上横。Player と NPC が共用
  PlaceBackground.gd       各場所の内装を _draw()（河川敷・境内・通り・座敷）
  HUD.tscn / HUD.gd         常時UI（カレンダー＋操作プロンプト）。Autoload で全シーンに表示
assets/fonts/
  NotoSansJP-Regular.ttf    日本語フォント（OFL 1.1）
```

## 設計の考え方（初心者向けメモ）

UIをボタン→1画面探索→2階層マップと変えても、**日付や好感度を持つ `GameState` は一切
変えていません。** これが「状態（モデル）と表示（ビュー）を分ける」設計の効き目です。

- **状態は `GameState` が唯一の持ち主。** 日付・時間帯・好感度・フラグと、
  `午前 → 午後 → 夜 →（めくる）→ 翌朝` を進める状態機械。ここはUIもマップも知らない。
- **シーンをまたいで生き残るもの＝ Autoload。** `GameState`（状態）・`Nav`（遷移）・
  `HUD`（常時UI）は Autoload なので、街と場所を行き来しても中身が保たれる。
  だから街↔場所の遷移は「シーンを差し替えるだけ」で成立する。
- **層ごとに操作を分ける。** 街(Town)は歩かない選択画面（カーソルで行き先を選ぶ）。
  場所(Place)は歩ける探索画面で、歩行系の共通土台 `ExploreMap`（プレイヤー配置・対象への
  出入り検知・入力受け）を継承する。操作キーの登録は `Controls`（Autoload）に集約。
- **シグナルで疎結合に。** 状態が変わると `GameState` が `day_changed` / `phase_changed`
  を emit し、`HUD` が受けて表示し直す。UIを増やしても購読するだけ。

## これから足していく（設計に沿った次の一手）

- [x] ドット絵：人物を `AnimatedSprite2D`（コード生成・歩き/待機アニメ）に、各場所に内装を追加
- [ ] 本物のアート：`CharacterArt` の生成を PNG スプライトシートに、`PlaceBackground` を `TileMapLayer` に差し替え
- [x] 会話中の選択肢（返答で好感度／フラグが動く。`then` で会話が枝分かれ）
- [x] フラグ／好感度によるエンディング分岐（`Endings.pick`）＋周回セーブ（全到達で裏エンド）
- [ ] 会話の分岐：日付・好感度・フラグでセリフ自体を変える（`Dialogues.for_location` に条件を追加）
- [x] タイトル画面（はじめる／エンディング記録＝図鑑／記録を消す）
- [x] 中盤の A/B/C（立場）を route 別に選び、エンディングの主軸に反映（`GameState.stance{}`）
- [x] 三ルート共通の器（`Routes`＋`Story`）＝ルート追加はデータ追加で済む（コード分岐なし）
      ／相手の枠を選んだときだけ、前提フラグ・関係値・時期を満たした次の1件が進む
- [x] 共通背景タイムライン（`Timeline`）＝中盤で球磨が河口へ傾く転機を全ルート共通の背景フラグに
      し、各ルートの節目台本が `if_flag` で描写の深さを出し分ける（§2）
- [x] 葵の遍在遭遇（`Story` が日替わりの居場所で軽く差し込む。枠を消費しない。§3）
- [x] 三ルート対応のエンディング判定（立場×関係値×到達節目数で着地1〜3／記録者フォールバック）
- [ ] 各ルートの中盤・終盤の節目テキストの作り込み（現状は仮置き）／特別な夜（花火・祭り）
- [ ] 日付・好感度で日常会話(filler)のセリフを段階変化（`Dialogues.route_filler` に条件を追加）
- [ ] カメラ追従（`Camera2D`）と各場所マップの作り込み（今は1画面・仮配置）

> **BGM／効果音**：音源ファイルが無くても、`AudioManager` がコードで合成して鳴らします。
> 本物に差し替えるときは `assets/audio/` に置くだけ（`bgm.ogg`、`blip/confirm/cancel/talk/page.wav`）。
> あればそちらを優先します。

> **エンディングを手早く試すには**：`autoload/GameState.gd` の `TOTAL_DAYS` を一時的に
> 小さく（例：2）すると、数日で 8/31 に到達してエンディングを確認できます。
> 到達エンドは `user://save.cfg` に残るので、全ノーマル到達で裏エンドが流れます。
- [ ] 特別な夜イベント（`夜` フェーズで場所へ入れるように分岐）
- [ ] フラグ／関係値によるエンディング分岐
- [ ] 周回をまたぐセーブ記録（到達エンド一覧 → 全到達で裏エンド解放）

詳しい物語は `docs/球磨ルート_プロット.md` を参照（※プロット文書は別途追加）。
